# Dev-only edge base. `wxt dev` runs in development mode, so the content/popup/
# background code reads this via import.meta.env.WXT_EDGE_BASE and targets the
# local portless CDN instead of production. Never shipped in a production build.
WXT_EDGE_BASE=https://cdn.auraimage.localhost
